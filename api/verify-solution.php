<?php
// 如果這個檔案存在，請更新所有 API 請求頭
// 從 "OpenAI-Beta: assistants=v1" 更改為 "OpenAI-Beta: assistants=v2"

// 在任何 curl_setopt_array 呼叫中替換此頭部
// 例如：
/*
curl_setopt_array($curl, [
    // ... 其他設置 ...
    CURLOPT_HTTPHEADER => [
        "Authorization: Bearer " . $apiKey,
        "Content-Type: application/json",
        "OpenAI-Beta: assistants=v2"  // 已更新為 v2
    ],
]);
*/
?>
